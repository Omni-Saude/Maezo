"""Integration workers - function-based (TOPIC + execute pattern).

Workers in this package are discovered by the registry via their TOPIC
module attribute. They do NOT export BaseExternalTaskWorker subclasses.
"""
