{
  "name": "bundle_surgical",
  "archetype": "BUNDLING",
  "description": "Surgical procedure bundling rules",
  "version": "1.0.0",
  "hit_policy": "FIRST",
  "inputs": [
    {"name": "primary_code", "type": "string", "required": true},
    {"name": "secondary_code", "type": "string", "required": true},
    {"name": "same_act", "type": "boolean", "required": true}
  ],
  "outputs": [
    {"name": "is_bundled", "type": "boolean"},
    {"name": "bundle_price", "type": "number"},
    {"name": "bundle_code", "type": "string"}
  ],
  "required_inputs": ["primary_code", "secondary_code", "same_act"],
  "example_rules": []
}
