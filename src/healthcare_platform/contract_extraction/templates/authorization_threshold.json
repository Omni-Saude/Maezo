{
  "name": "authorization_threshold",
  "archetype": "AUTHORIZATION",
  "description": "Prior authorization threshold rules",
  "version": "1.0.0",
  "hit_policy": "FIRST",
  "inputs": [
    {"name": "procedure_code", "type": "string", "required": true},
    {"name": "amount", "type": "number", "required": true},
    {"name": "payer_id", "type": "string", "required": true}
  ],
  "outputs": [
    {"name": "requires_auth", "type": "boolean"},
    {"name": "auth_type", "type": "string"},
    {"name": "urgency_level", "type": "string"}
  ],
  "required_inputs": ["procedure_code", "amount", "payer_id"],
  "example_rules": []
}
