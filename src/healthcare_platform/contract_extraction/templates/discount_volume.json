{
  "name": "discount_volume",
  "archetype": "PRICING",
  "description": "Volume-based discount tiers for high-volume payers",
  "version": "1.0.0",
  "hit_policy": "FIRST",
  "inputs": [
    {"name": "payer_id", "type": "string", "required": true},
    {"name": "monthly_volume", "type": "number", "required": true}
  ],
  "outputs": [
    {"name": "discount_pct", "type": "number"},
    {"name": "effective_multiplier", "type": "number"},
    {"name": "discount_type", "type": "string"}
  ],
  "required_inputs": ["payer_id", "monthly_volume"],
  "example_rules": []
}
