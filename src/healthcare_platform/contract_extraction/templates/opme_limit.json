{
  "name": "opme_limit",
  "archetype": "PRICING",
  "description": "OPME (medical devices) price limit rules",
  "version": "1.0.0",
  "hit_policy": "FIRST",
  "inputs": [
    {"name": "item_code", "type": "string", "required": true},
    {"name": "reference_price", "type": "number", "required": true},
    {"name": "payer_id", "type": "string", "required": true}
  ],
  "outputs": [
    {"name": "max_allowed", "type": "number"},
    {"name": "limit_percentage", "type": "number"},
    {"name": "limit_type", "type": "string"}
  ],
  "required_inputs": ["item_code", "reference_price", "payer_id"],
  "example_rules": []
}
