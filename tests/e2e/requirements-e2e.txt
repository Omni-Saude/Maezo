# Dependências para testes E2E do ambiente local MAEZO
# Instale com: pip install -r tests/e2e/requirements-e2e.txt

# HTTP client (já no pyproject.toml)
httpx>=0.27,<1.0

# PostgreSQL async (já no pyproject.toml)
asyncpg>=0.29,<1.0

# Test framework
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=5.0,<6.0
pytest-timeout>=2.3,<3.0

# Relatórios HTML opcionais
pytest-html>=4.1,<5.0
