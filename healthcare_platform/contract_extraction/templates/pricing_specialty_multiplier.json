{
  "name": "pricing_specialty_multiplier",
  "archetype": "PRICING",
  "description": "Specialty-based price multiplier for procedures",
  "version": "1.0.0",
  "hit_policy": "FIRST",
  "inputs": [
    {"name": "specialty", "type": "string", "required": true},
    {"name": "payer_id", "type": "string", "required": true},
    {"name": "base_amount", "type": "number", "required": true}
  ],
  "outputs": [
    {"name": "multiplier", "type": "number"},
    {"name": "adjusted_amount", "type": "number"},
    {"name": "specialty_code", "type": "string"}
  ],
  "required_inputs": ["specialty", "payer_id", "base_amount"],
  "example_rules": []
}
