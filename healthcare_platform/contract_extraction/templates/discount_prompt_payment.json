{
  "name": "discount_prompt_payment",
  "archetype": "PRICING",
  "description": "Prompt payment discount based on payment terms",
  "version": "1.0.0",
  "hit_policy": "FIRST",
  "inputs": [
    {"name": "payer_id", "type": "string", "required": true},
    {"name": "payment_days", "type": "number", "required": true},
    {"name": "monthly_volume", "type": "number", "required": false}
  ],
  "outputs": [
    {"name": "discount_pct", "type": "number"},
    {"name": "effective_multiplier", "type": "number"},
    {"name": "discount_type", "type": "string"}
  ],
  "required_inputs": ["payer_id", "payment_days"],
  "example_rules": []
}
